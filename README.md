# MaidEase Backend (FastAPI)

The MaidEase backend powers all core features of the platform — authentication, maid profiles, bookings, reviews, and notifications.  
It is built using FastAPI, PostgreSQL, and SQLAlchemy, with JWT-based authentication and a simple service-layer structure.

---

## Tech Stack

- FastAPI (Python 3.11+)
- PostgreSQL + SQLAlchemy
- Alembic (migrations)
- Uvicorn
- Pytest (unit testing)
- Docker + Docker Compose

---

## How to Clone and Run the Backend Locally

Below are the exact steps to get the backend running on your machine.

### 1. Clone the Repository

```bash
git clone https://github.com/Hritik-Vj/maidease-backend
cd maidease-backend
